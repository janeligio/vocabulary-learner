{
  "name": "vocabulary-learner",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "concurrently": "^4.1.0",
    "moment": "^2.24.0",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-hotkeys": "^1.1.4",
    "react-moment": "^0.8.4",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "cd server && npm run start",
    "start-old": "react-scripts start",
    "full": "concurrently \"react-scripts start\" \"cd server && npm run start\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install && npm run build"

  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "proxy": "http://localhost:5001",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
